// Environment configuration for production mode
export const environment = {
  // Indicates that the app is running in production
  production: true,

  // Base URL of the production backend API
  apiUrl: 'https://mindcraftapi.onrender.com'
};